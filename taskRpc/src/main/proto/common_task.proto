syntax = "proto3";

option java_multiple_files = true;
option java_package = "com.jal.crawler.proto.task";

package rpc;

enum TaskType {
    ADD = 0;
    STOP = 1;
    UPDATE = 2;
    FINISH = 3;
    DESTROY = 4;
}

enum OPStatus {
    SUCCEED = 0;
    FAILD = 1;
    REPEATED = 2;
}